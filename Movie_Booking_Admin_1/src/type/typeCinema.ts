export type CinemaType = {
    cinemaId: number,
    address: string,
    cinemaName: string,
    city: string,
}
export type CinemaTypeRequest = {
    address: string,
    cinemaName: string,
    city: string,
}
